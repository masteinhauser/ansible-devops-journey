---
- name: is this thing on?
  hosts: all
  tasks:
    - command: uname -a
      register: uname
    - debug: var=uname

- name: build chat server
  hosts: chatsrv
  roles:
    - nginx
    - simple-chat
